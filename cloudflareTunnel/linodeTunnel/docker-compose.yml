version: '3.8'
services:
  linode_Tunnel:
    image: cloudflare/cloudflared:latest
    restart: unless-stopped
    container_name: linodeTunnel
    command: tunnel --no-autoupdate run
    env_file:
      - .env
    environment:
      - TUNNEL_TOKEN=${cf_tunnel_token}
